<%= form_for [project, option], html: {multipart: true} do |f| %>
  <%= f.file_field :image %>
  <%= f.text_field :description, placeholder: 'Add an option...' %>
  <%= f.submit "Create option", class: "btn optionbutton" %>
<% end %>